apiVersion: v1
kind: Service
metadata:
  name: {{ include "imperium-news-app.fullname" . }}
  labels:
    {{- include "imperium-news-app.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "imperium-news-app.selectorLabels" . | nindent 4 }}
