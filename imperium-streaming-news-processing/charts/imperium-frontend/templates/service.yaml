apiVersion: v1
kind: Service
metadata:
  name: {{ include "imperium-frontend.fullname" . }}
  labels:
    {{- include "imperium-frontend.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "imperium-frontend.selectorLabels" . | nindent 4 }}
