apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "kafka-ui.fullname" . }}
  labels:
    {{- include "kafka-ui.labels" . | nindent 4 }}
spec:
  replicas: 1
  selector:
    matchLabels:
      {{- include "kafka-ui.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      labels:
        {{- include "kafka-ui.selectorLabels" . | nindent 8 }}
    spec:
      containers:
        - name: {{ .Chart.Name }}
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag | default .Chart.AppVersion }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          ports:
            - name: http
              containerPort: 8080
              protocol: TCP
          env:
            - name: KAFKA_CLUSTERS_0_NAME
              value: {{ .Values.kafka.clusterName | quote }}
            - name: KAFKA_CLUSTERS_0_BOOTSTRAPSERVERS
              value: {{ .Values.kafka.bootstrapServers | quote }}
            {{- if .Values.kafka.schemaRegistry }}
            - name: KAFKA_CLUSTERS_0_SCHEMAREGISTRY
              value: {{ .Values.kafka.schemaRegistry | quote }}
            {{- end }}
            {{- if .Values.kafka.kafkaConnectAddress }}
            - name: KAFKA_CLUSTERS_0_KAFKACONNECT_0_NAME
              value: "imperium-connect"
            - name: KAFKA_CLUSTERS_0_KAFKACONNECT_0_ADDRESS
              value: {{ .Values.kafka.kafkaConnectAddress | quote }}
            {{- end }}
          resources:
            {{- toYaml .Values.resources | nindent 12 }}
